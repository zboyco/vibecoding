---
name: golang-guidelines
description: Golang开发指南，包括文件修改边界、包用法不明确时的处理方式等。
---

# Go 开发约束

## 包用法不明确时

- 先使用 `go get` 下载指定版本的包。
- 再使用 `mcp godoc` 查看包、函数或方法用法。
- 优先依据文档确认接口签名与行为，再进行实现。

## 文件修改边界

- 不修改任何以 `_generated.go` 结尾的文件。
- 将改动集中在可维护的手写代码文件中。
